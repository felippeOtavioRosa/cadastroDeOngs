import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); //React.dom vai renderizar (por em tela) os elementos da div com id root
 

